<div class="card m-3 col-5 col-sm-10">
    <a href="/api/blogs/{{id}}" class="card-header fs-3 fw-bold">{{title}}</a>
    <div class="card-body">
        <blockquote class="blockquote mb-0">
            <p>{{description}}.</p>
        </blockquote>
    </div>
</div>